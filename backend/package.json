{
  "name": "chatbot-localrag-backend",
  "version": "1.0.0",
  "description": "Backend local (Express) para chatbot con FAQ + caché semántico + fallback a modelo local (Ollama qwen3:8b) y panel de revisión de respuestas.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
